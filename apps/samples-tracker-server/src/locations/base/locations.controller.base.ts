/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { LocationsService } from "../locations.service";
import { LocationsCreateInput } from "./LocationsCreateInput";
import { Locations } from "./Locations";
import { LocationsFindManyArgs } from "./LocationsFindManyArgs";
import { LocationsWhereUniqueInput } from "./LocationsWhereUniqueInput";
import { LocationsUpdateInput } from "./LocationsUpdateInput";
import { TrackingEventFindManyArgs } from "../../trackingEvent/base/TrackingEventFindManyArgs";
import { TrackingEvent } from "../../trackingEvent/base/TrackingEvent";
import { TrackingEventWhereUniqueInput } from "../../trackingEvent/base/TrackingEventWhereUniqueInput";

export class LocationsControllerBase {
  constructor(protected readonly service: LocationsService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Locations })
  async createLocations(
    @common.Body() data: LocationsCreateInput
  ): Promise<Locations> {
    return await this.service.createLocations({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        typeField: true,
        address: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Locations] })
  @ApiNestedQuery(LocationsFindManyArgs)
  async locationsItems(@common.Req() request: Request): Promise<Locations[]> {
    const args = plainToClass(LocationsFindManyArgs, request.query);
    return this.service.locationsItems({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        typeField: true,
        address: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Locations })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async locations(
    @common.Param() params: LocationsWhereUniqueInput
  ): Promise<Locations | null> {
    const result = await this.service.locations({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        name: true,
        typeField: true,
        address: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Locations })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateLocations(
    @common.Param() params: LocationsWhereUniqueInput,
    @common.Body() data: LocationsUpdateInput
  ): Promise<Locations | null> {
    try {
      return await this.service.updateLocations({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          typeField: true,
          address: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Locations })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteLocations(
    @common.Param() params: LocationsWhereUniqueInput
  ): Promise<Locations | null> {
    try {
      return await this.service.deleteLocations({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          name: true,
          typeField: true,
          address: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/trackingEvents")
  @ApiNestedQuery(TrackingEventFindManyArgs)
  async findTrackingEvents(
    @common.Req() request: Request,
    @common.Param() params: LocationsWhereUniqueInput
  ): Promise<TrackingEvent[]> {
    const query = plainToClass(TrackingEventFindManyArgs, request.query);
    const results = await this.service.findTrackingEvents(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        timestamp: true,
        action: true,
        reference: true,

        location: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/trackingEvents")
  async connectTrackingEvents(
    @common.Param() params: LocationsWhereUniqueInput,
    @common.Body() body: TrackingEventWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trackingEvents: {
        connect: body,
      },
    };
    await this.service.updateLocations({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/trackingEvents")
  async updateTrackingEvents(
    @common.Param() params: LocationsWhereUniqueInput,
    @common.Body() body: TrackingEventWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trackingEvents: {
        set: body,
      },
    };
    await this.service.updateLocations({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/trackingEvents")
  async disconnectTrackingEvents(
    @common.Param() params: LocationsWhereUniqueInput,
    @common.Body() body: TrackingEventWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trackingEvents: {
        disconnect: body,
      },
    };
    await this.service.updateLocations({
      where: params,
      data,
      select: { id: true },
    });
  }
}
